import localFont from 'next/font/local';

// Define the SVN-Hemi Head font
export const svnHemiHead = localFont({
  src: '../../public/fonts/SVN-Hemi-Head.ttf',
  variable: '--font-svn-hemi-head'
});
